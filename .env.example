# GitHub repository configuration for fetching remote data
# These are optional - defaults are already set in the code
VITE_GITHUB_OWNER=SquaredCub
VITE_GITHUB_REPO=arcraiders-data
VITE_GITHUB_BRANCH=main
